// LEGACY EXAMPLE -- DOES NOT RUN AS WRITTEN.
// This targets an older script contract (`payload` in / `result` out).
// The current contract reads `input` and assigns `output`; see README.md
// in this directory. Kept for reference while it is ported.

// Import the 'text' module for regular expression operations
text := import("text")

// Define regular expression patterns for various PII
email_pattern := `[\w\.-]+@[\w\.-]+\.\w+`

// Define the function to sanitize PII in the input string
filter := func(input) {
    // Replace email addresses
    input = text.re_replace(email_pattern, input, "[REDACTED EMAIL]")

    return input
}

result := filter(payload)
