# Generated by: gromit policy

# Check the documentation at http://goreleaser.com
# This project needs CGO_ENABLED=1 and the cross-compiler toolchains for
# - arm64
# - amd64
version: 2
builds:
  - id: std-amd64
    env:
      - NOP=nop # ignore this, it is jsut to avoid a complex conditional in the templates
      - CC=gcc
    ldflags:
      - -X github.com/TykTechnologies/ai-studio/internal/build.Version={{.Version}}
      - -X github.com/TykTechnologies/ai-studio/internal/build.Commit={{.FullCommit}}
      - -X github.com/TykTechnologies/ai-studio/internal/build.BuildDate={{.Date}}
      - -X github.com/TykTechnologies/ai-studio/internal/build.BuiltBy=goreleaser
    goos:
      - linux
    goarch:
      - amd64
    binary: tyk-ai-studio
  - id: std-arm64
    env:
      - NOP=nop # ignore this, it is jsut to avoid a complex conditional in the templates
      - CC=aarch64-linux-gnu-gcc
    ldflags:
      - -X github.com/TykTechnologies/ai-studio/internal/build.Version={{.Version}}
      - -X github.com/TykTechnologies/ai-studio/internal/build.Commit={{.FullCommit}}
      - -X github.com/TykTechnologies/ai-studio/internal/build.BuildDate={{.Date}}
      - -X github.com/TykTechnologies/ai-studio/internal/build.BuiltBy=goreleaser
    goos:
      - linux
    goarch:
      - arm64
    binary: tyk-ai-studio
  - id: std-s390x
    env:
      - NOP=nop # ignore this, it is jsut to avoid a complex conditional in the templates
      - CC=s390x-linux-gnu-gcc
    ldflags:
      - -X github.com/TykTechnologies/ai-studio/internal/build.Version={{.Version}}
      - -X github.com/TykTechnologies/ai-studio/internal/build.Commit={{.FullCommit}}
      - -X github.com/TykTechnologies/ai-studio/internal/build.BuildDate={{.Date}}
      - -X github.com/TykTechnologies/ai-studio/internal/build.BuiltBy=goreleaser
    goos:
      - linux
    goarch:
      - s390x
    binary: tyk-ai-studio
nfpms:
  - id: std
    vendor: "Tyk Technologies Ltd"
    homepage: "https://tyk.io"
    maintainer: "Tyk <info@tyk.io>"
    description: Tyk AI Studio - AI Management Platform for LLM Applications
    package_name: tyk-ai-studio
    file_name_template: "{{ .ConventionalFileName }}"
    ids:
      - std-amd64
      - std-arm64
      - std-s390x
    formats:
      - deb
      - rpm
    contents:
      - src: "README.md"
        dst: "/opt/share/docs/tyk-ai-studio/README.md"
      - src: "ci/install/*"
        dst: "/opt/tyk-ai-studio/install"
      - src: ci/install/inits/systemd/system/tyk-ai-studio.service
        dst: /lib/systemd/system/tyk-ai-studio.service
      - src: ci/install/inits/sysv/init.d/tyk-ai-studio
        dst: /etc/init.d/tyk-ai-studio
    scripts:
      preinstall: "ci/install/before_install.sh"
      postinstall: "ci/install/post_install.sh"
      postremove: "ci/install/post_remove.sh"
    bindir: "/opt/tyk-ai-studio"
    rpm:
      scripts:
        posttrans: ci/install/post_trans.sh
      signature:
        key_file: tyk.io.signing.key
    deb:
      signature:
        key_file: tyk.io.signing.key
        type: origin
publishers:
  - name: std
    ids:
      - std
    env:
      - PACKAGECLOUD_TOKEN={{ .Env.PACKAGECLOUD_TOKEN }}
    cmd: packagecloud publish --debvers "{{ .Env.DEBVERS }}" --rpmvers "{{ .Env.RPMVERS }}" tyk/tyk-ai-studio-unstable {{ .ArtifactPath }}
dockers:
  # Build tykio/tyk-ai-studio std (amd64)
  - ids:
      - std-amd64
    image_templates:
      - "tykio/tyk-ai-studio:{{.Tag}}-std-amd64"
    build_flag_templates:
      - "--build-arg=PORTS=8080"
      - "--build-arg=BUILD_PACKAGE_NAME=tyk-ai-studio"
      - "--platform=linux/amd64"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
    use: buildx
    goarch: amd64
    goos: linux
    dockerfile: ci/Dockerfile.std
    extra_files:
      - "ci/install/"
      - "README.md"
      - "dist/"
  # Build tykio/tyk-ai-studio std (arm64)
  - ids:
      - std-arm64
    image_templates:
      - "tykio/tyk-ai-studio:{{.Tag}}-std-arm64"
    build_flag_templates:
      - "--build-arg=PORTS=8080"
      - "--build-arg=BUILD_PACKAGE_NAME=tyk-ai-studio"
      - "--platform=linux/arm64"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
    use: buildx
    goarch: arm64
    goos: linux
    dockerfile: ci/Dockerfile.std
    extra_files:
      - "ci/install/"
      - "README.md"
      - "dist/"
  # Build tykio/tyk-ai-studio std (s390x)
  - ids:
      - std-s390x
    image_templates:
      - "tykio/tyk-ai-studio:{{.Tag}}-std-s390x"
    build_flag_templates:
      - "--build-arg=PORTS=8080"
      - "--build-arg=BUILD_PACKAGE_NAME=tyk-ai-studio"
      - "--platform=linux/s390x"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
    use: buildx
    goarch: s390x
    goos: linux
    dockerfile: ci/Dockerfile.std
    extra_files:
      - "ci/install/"
      - "README.md"
      - "dist/"
docker_manifests:
  # Multi-arch manifest for tykio/tyk-ai-studio std
  - name_template: tykio/tyk-ai-studio:{{ .Tag }}
    image_templates:
      - tykio/tyk-ai-studio:{{`{{ .Tag }}`}}-std-amd64
      - tykio/tyk-ai-studio:{{`{{ .Tag }}`}}-std-arm64
      - tykio/tyk-ai-studio:{{`{{ .Tag }}`}}-std-s390x
  - name_template: tykio/tyk-ai-studio:v{{ .Major }}.{{ .Minor }}{{.Prerelease}}
    image_templates:
      - tykio/tyk-ai-studio:{{`{{ .Tag }}`}}-std-amd64
      - tykio/tyk-ai-studio:{{`{{ .Tag }}`}}-std-arm64
      - tykio/tyk-ai-studio:{{`{{ .Tag }}`}}-std-s390x
  - name_template: tykio/tyk-ai-studio:v{{ .Major }}{{.Prerelease}}
    image_templates:
      - tykio/tyk-ai-studio:{{`{{ .Tag }}`}}-std-amd64
      - tykio/tyk-ai-studio:{{`{{ .Tag }}`}}-std-arm64
      - tykio/tyk-ai-studio:{{`{{ .Tag }}`}}-std-s390x
# This disables archives
archives:
  - formats: ['binary']
    allow_different_binary_count: true
checksum:
  disable: true
release:
  disable: true
  github:
    owner: TykTechnologies
    name: ai-studio
  prerelease: auto
  draft: true
  name_template: "{{.ProjectName}}-v{{.Version}}"
