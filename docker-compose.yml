# =============================================================================
# DEPRECATED: This docker-compose.yml is deprecated.
#
# Please use the new development environment instead:
#   make dev          - Start minimal development environment
#   make dev-full     - Start full stack with Gateway and Plugins
#   make dev-ent      - Start enterprise development environment
#
# The new setup provides:
#   - Hot reloading for Go code (via Air)
#   - PostgreSQL database (matches production)
#   - Multi-component support (Studio, Gateway, Plugins)
#   - Easy edition switching (CE/ENT)
#
# See dev/README.md for full documentation.
# =============================================================================

version: "3.8"
services:
  dev:
    platform: linux/${TARGETARCH:-amd64}
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - /app/ui/admin-frontend/node_modules
      - ./midsommar.db:/app/midsommar.db # Bind mount SQLite database file
      - ./.env:/app/.env # Mount .env file
    ports:
      - "3000:3000" # Frontend
      - "8080:8080" # Backend
    environment:
      - CHOKIDAR_USEPOLLING=true
    stdin_open: true
    tty: true
